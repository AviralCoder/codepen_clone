import React from "react";

const Navbar = ({ title }) => {
    return (
        <section className="navbar">
            <h1>{title}</h1>

            <a href=""></a>
        </section>
    );
};

export default Navbar;
